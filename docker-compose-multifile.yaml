# docker-compose-include.yaml
#name: dockercomposeexample_multifile
services:
  web:
    container_name: "web-3"
    build:
      context:    .
      dockerfile: Dockerfile
    ports:
      - "8003:5000"
